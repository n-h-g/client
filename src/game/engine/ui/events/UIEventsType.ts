export enum UIEventsType {
    INVENTORY = 'inventory',
    CATALOGUE = 'catalogue',
    NAVIGATOR = 'navigator',
    LOADER = 'loader',
    PREVIEWBOX = 'preview_box',
    HOTELVIEW = 'hotelview',
    ROOM = 'room'
}